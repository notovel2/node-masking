{
  "name": "node-masking",
  "version": "1.0.0",
  "main": "dist/index.js",
  "license": "MIT",
  "scripts": {
    "build": "rm -rf dist && tsc",
    "prepublish": "npm run build",
    "start": "npm run build && node dist/index.js"
  },
  "devDependencies": {
    "typescript": "^4.2.4"
  }
}
